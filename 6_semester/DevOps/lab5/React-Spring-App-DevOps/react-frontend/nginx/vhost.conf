server {
    listen 4200;

    root /var/www/html;
    
    location / {
        index index.html;
    }

    # You may need this to prevent return 404 recursion.
    location = /404.html {
        internal;
    }
}